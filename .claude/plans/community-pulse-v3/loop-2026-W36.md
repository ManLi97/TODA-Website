# Testlauf-Loop — Woche 2026-W36 (06.09.2026)

Rubrik: `quality-rubrik.md`. Spend: `spend.md`. Alle Zahlen aus `mining_runs` / `topic_signals` /
`topic_classifications` (Read-only-MCP) bzw. den CLI-Logs im Session-Scratchpad.

## Lauf 1 — Erhebung (18:04–18:12 UTC)

- `pnpm mining:sync` (voll, Lock `battery`, Job `cf4e05f0…`): **77 Runs, 76 succeeded, 1 failed**,
  1251 neue Zeilen, 963 Bodies (> 30 Tage) geschwärzt. DeepAPI-Debit 6,15 $ (Holds waren 9,82 $).
- Failed: `ig-hashtags/taetowierer` — 17 Roh-Items, alle > 14 Tage alt → Frische-Filter lief im
  Mapper und zählte als „no mappable items". **Fix** (`603b4b7`): Filter im Ingest, zählt als
  `dedupedCount`; Replay (idempotent, frei) → succeeded, 17 gefiltert, 0 neu.
- Dünn: `ig-accounts` 6 Items / 2 neu (12 Accounts, `since: week`), `yt-channels` 9 Videos
  (`since: month`, 2 Kanäle), `reviews/trustpilot/tattoodo` 0 (DeepAPI extract: `listState
  no_results`, kostenfrei — Trustpilot liefert dem Extractor keine Reviews; Fallback `browser/act`
  wäre 0,125–0,75 $/Task → Entscheidung Tomek), `web/*` 40 von 44 Treffern bereits bekannt (Dedupe).
- Stark: `yt-comments/*` 5 Videos × 100 (331 neu; 2 Videos waren schon 29.08. gescrapt → 169
  dedupliziert), `reddit-search/de` 35 neu, `reddit-comments/*` 58, `fb-groups/*` 66 neu (3
  Cross-Posts kollabiert), `tiktok-comments/*` 124 neu, Reviews 241 neu (Apple 168, Play 74).
- **E4 Nur-Neues-Beweis:** `reviews/apple/1526690381/gb --fresh` → 28 raw, 28 dedupliziert,
  0 neu, `succeeded`, kein Fehler.

## Lauf 1 — Verdichtung

- Stichprobe 1: 50 Zeilen mit Prompt v3.0 (2 Calls, 0,27 $). Handprüfung (Web/TikTok-lastig,
  weil `ingested_at`-Reihenfolge): Sprache 50/50, Anonymisierung sauber („[…]" statt Namen/
  Handles), Cluster plausibel (business-studio, career-entry, no-shows, deposits-anzahlung,
  pricing, regulation-hygiene), 5 sinnvolle `cluster_proposal`s. Schwäche: 6 von 48 Web-
  Ratgeber-Artikel `signal_type other` + `is_discussion true` (inkonsistent, senkt %useful nicht
  zu Unrecht — SEO-Artikel sind keine Community-Stimme). → **Prompt v3.1**: Konsistenzregel
  (other/promo ⇒ keine Diskussion; news = echte Neuigkeit). `--reclassify v3.0 --week 2026-W36`
  löscht die 50 Zeilen und verdichtet die ganze Woche (1871 Zeilen) neu — 4 Calls parallel.
- Stichprobe 2 (v3.1, 18 zufällige Nicht-Web-Zeilen: YT-/TikTok-Kommentare, Reddit, IG-Captions):
  **18/18 vertretbar** — Lehrlings-Beschwerde → `career-entry`/complaint, Stencil-Tipp →
  `technique-equipment`/experience, Showcase-Posts → promo/other ohne Diskussion, „darf ich dir
  privat schreiben" → off_topic, koreanische Caption → `ko`. Nach 100 v3.1-Zeilen: 1 × other+
  discussion (vorher 6/48).
- Lock-Beweis (D9): zweiter `--enrich` während des laufenden Jobs → `JobLockedError` (CLI) und
  `409` (Route `pulse-worker?step=enrich`).

## Routen (E7, lokal `next dev -p 3111`, temporärer CRON_SECRET)

- ohne Token → 401 (`mining-sync`, `pulse-worker`); `?step=nope` → 400;
  `?step=digest` bei offenen Zeilen → 200 `{skipped: "1671 rows still open"}`;
  `?step=enrich` bei laufendem CLI-Job → 409. Kette end-to-end: siehe unten.

## Kette end-to-end (E7, 18:21–18:22 UTC, lokal)

`GET /api/cron/mining-sync` → 202 (`battery`, attempts 2) → Batterie in `after()` (59 Runs,
alle DeepAPI-Requests als Replay derselben Idempotency-Keys, Balance unverändert 39,69 $) →
`[pulse-chain] trigger comments → 202` → `comments` succeeded (18 Runs, 582 Items, Replay) →
`trigger enrich → 202` → `enrich` **failed** (`pulse_jobs.error`: Anthropic „credit balance is
too low"). Kette, Locks, Trigger und Fehler-Sichtbarkeit funktionieren; Digest folgt nach dem
Anthropic-Top-up. Hinweis: der Replay zählt SerpApi-Quota erneut (8 Suchen).

## Stand der Verdichtung (18:27 UTC) — BLOCKIERT

- 1000 von 1871 W36-Zeilen mit v3.1 klassifiziert, 871 offen; Anthropic-Guthaben aufgebraucht
  (Fehler 400 `invalid_request_error`). Enrich-Job `failed` (attempts 3) — Wiederaufnahme mit
  `pnpm mining:sync --enrich --week 2026-W36`, dann `--digest --week 2026-W36`, dann `--quality`.
- Anthropic-Kosten der 1000 Zeilen: geschätzt ≈ 5,5 $ (Hochrechnung aus der 50er-Stichprobe:
  0,273 $ je 50 Zeilen; exakte Usage ging mit dem Abbruch verloren).

## Qualitätsreport auf Teilstand (1000/1871 klassifiziert; W36 enthält auch die v2-Cron-Runs vom 31.08.)

Ebene 2 bereits erfüllt: useful_de 426 (≥ 300), Cluster mit Trend-Gate 14 (≥ 5), Fragen de 44
(≥ 40), Beschwerden+Wünsche de 59 (≥ 40), Review-Zeilen 239, Identitätsfelder 0, Zitate 660.
Ebene 1 (rote Zellen, vorläufig): `fb-groups` %useful 0 / %spam 75 (Job-/Guest-Spot-Posts =
promo) · `ig-hashtags` %useful 13 (Showcase) · `serp` %useful 24 (PAA-Fragen tragen, Trend-
Queries sind „other") · `reviews` %DE 40 (gb/us tragen das Volumen, wie erwartet) · `web` %new
57 · `yt-comments` %artist 7 (Endkunden-Kommentare unter DE-Videos — richtig gelabelt) ·
`tiktok-search` %DE 50. Vollständige Tabelle nach dem Rest-Enrich.
